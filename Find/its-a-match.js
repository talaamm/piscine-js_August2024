// Matches 'hi' anywhere in the string
const normal = /hi/;

// Matches 'hi' only at the beginning of the string
const begin = /^hi/;

// Matches 'hi' only at the end of the string
const end = /hi$/;

// Matches exactly 'hi' (only 'hi' with nothing else)
const beginEnd = /^hi$/;